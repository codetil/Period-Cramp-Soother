import { Component } from "@/components/component/component";
import Vix from "@/components/vix"; // Correctly import the component 'vix'


export default function home() {
   
  
    return (
      <main>
        <Vix/> {/* Use the correct component name */}
      </main>
    )
  }